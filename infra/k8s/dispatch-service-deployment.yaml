---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispatch-service
  namespace: foodrush
  labels:
    app.kubernetes.io/name: dispatch-service
    app.kubernetes.io/part-of: foodrush
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: dispatch-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dispatch-service
        app.kubernetes.io/part-of: foodrush
        app.kubernetes.io/component: backend
    spec:
      serviceAccountName: dispatch-service
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dispatch-service
          image: ${REGION}.ocir.io/${TENANCY}/foodrush-prod/dispatch-service:latest
          ports:
            - name: http
              containerPort: 3003
              protocol: TCP
          env:
            - name: PORT
              value: "3003"
            - name: HOST
              value: "0.0.0.0"
            - name: KAFKA_CLIENT_ID
              valueFrom:
                configMapKeyRef:
                  name: foodrush-config
                  key: DISPATCH_KAFKA_CLIENT_ID
          envFrom:
            - configMapRef:
                name: foodrush-config
            - secretRef:
                name: foodrush-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: dispatch-service
---
apiVersion: v1
kind: Service
metadata:
  name: dispatch-service
  namespace: foodrush
  labels:
    app.kubernetes.io/name: dispatch-service
    app.kubernetes.io/part-of: foodrush
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dispatch-service
  ports:
    - name: http
      port: 3003
      targetPort: http
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dispatch-service
  namespace: foodrush
  labels:
    app.kubernetes.io/name: dispatch-service
    app.kubernetes.io/part-of: foodrush
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dispatch-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dispatch-service
  namespace: foodrush
  labels:
    app.kubernetes.io/name: dispatch-service
    app.kubernetes.io/part-of: foodrush
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dispatch-service
